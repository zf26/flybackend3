<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pf.im.mapper.ImConversationMapper">

    <resultMap id="ImConversationResult" type="com.pf.im.domain.ImConversation">
        <id     property="id"               column="id"/>
        <result property="roomId"           column="room_id"/>
        <result property="conversationType" column="conversation_type"/>
        <result property="userIdA"         column="user_id_a"/>
        <result property="userIdB"         column="user_id_b"/>
        <result property="status"          column="status"/>
        <result property="remark"          column="remark"/>
        <result property="createTime"      column="create_time"/>
        <result property="updateTime"      column="update_time"/>
    </resultMap>

    <select id="selectByUserId" resultMap="ImConversationResult">
        SELECT
            id,
            room_id,
            conversation_type,
            user_id_a,
            user_id_b,
            status,
            remark,
            create_time,
            update_time
        FROM im_conversation
        WHERE (user_id_a = #{userId} OR user_id_b = #{userId})
        ORDER BY update_time DESC
    </select>

</mapper>
